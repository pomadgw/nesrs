{
  "name": "nesrs-scripts",
  "version": "1.0.0",
  "description": "",
  "main": "generate-implementation.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node ./generate-implementation.js && cargo fmt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^2.0.1"
  }
}
